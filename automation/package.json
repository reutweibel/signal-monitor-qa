{
  "name": "signal-monitor-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright test suite for the Signal Monitor QA home exercise (Part 2)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
